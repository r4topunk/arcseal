{
  "name": "@arcseal/scripts",
  "version": "0.0.0",
  "private": true,
  "description": "Operator scripts: testnet end-to-end run and an optional reference reveal CLI (not hosted)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "e2e:testnet": "tsx e2e-testnet.ts",
    "e2e:dry-run": "tsx e2e-testnet.ts --dry-run",
    "reveal": "tsx reveal-cli.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "biome check .",
    "format": "biome check --write ."
  },
  "dependencies": {
    "@arcseal/sdk": "workspace:*",
    "@arcseal/tlock": "workspace:*",
    "viem": "^2.56.7",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.14",
    "@types/node": "^24.13.5",
    "tsx": "^4.20.0",
    "typescript": "~5.9.3",
    "vitest": "^4.1.11"
  }
}
